[package]
name = "kondo"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
toml = "0.5"
serde = { version = "1.0", features = ["derive"] }
dirs = "5.0"
